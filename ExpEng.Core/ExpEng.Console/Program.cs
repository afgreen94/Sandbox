using System;
using System.Threading.Tasks;
using ExpEng.Core.Drivers;
namespace ExpEng.Console { public class Program { public static Task Main(string[] args) => Task.CompletedTask; } }//await new Driver().DriveAsync(args).ConfigureAwait(false); } }